name: bootstrap (deprecated)

# This workflow has been replaced by bootstrap-global.yml.
# Use bootstrap-global instead: Actions → bootstrap-global → Run workflow
on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Redirect to bootstrap-global
        run: |
          echo "::error::This workflow is deprecated. Use bootstrap-global.yml instead."
          exit 1
